import React,{Component} from 'react';
import classNames from 'classnames';

class Sinov extends Component{

    constructor(props){
       super(props)
       this.state={
           isButton:true
       }
    }

    render(){
  
        return(
            <div>
                sinov
                </div>
        )
    }
}

export default Sinov;
